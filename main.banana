puts("Monkey see monkey do! Banana");

let a = fn(x) {
    let b = fn(y) {
        if (y < 10) {
            return b(y + 1);
        } else {
            return y + 8;
        }
    };
    b(x)
};

let fib = fn(x) {
	if (x<2) {
	return 1;
	}
	return fib(x-1) + fib(x-2);
}

let b = [1, "hello", 99];

puts(fib(1));

